export const prerender = true;
export const ssr = true;
//export const trailingSlash = 'always'; //handles the problem that pages other than index.html (for example /analysis) are rendered to analysis.html, but creates a problem with relative paths.